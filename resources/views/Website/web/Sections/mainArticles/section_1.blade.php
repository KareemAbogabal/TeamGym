<section class="section-1">
    <div class="badge">{{ __('article.nav-store') }}</div>
    <h1 class="title">{{ __('article.section1_title') }}</h1>
    <p class="subtitle">{{ __('article.section1_subtitle') }}</p>
    <div class="tags">
      <div class="tag">{{ __('article.tags_workouts') }}</div>
      <div class="tag">{{ __('article.tags_nutrition') }}</div>
      <div class="tag">{{ __('article.tags_muscle_building') }}</div>
      <div class="tag">{{ __('article.tags_recovery') }}</div>
      <div class="tag">{{ __('article.tags_training_plans') }}</div>
    </div>
  </section>